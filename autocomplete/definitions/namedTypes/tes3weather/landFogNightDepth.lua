return {
	type = "value",
	description = [[Land-fog depth used at night. The controller interpolates toward this value from `landFogDayDepth` over `fogDepthChangeSpeed` hours after `sunsetHour`.]],
	valuetype = "number",
}