return {
	type = "value",
	description = [[Blend weight for `underwaterColor`: `0` keeps the normal weather color, and `1` replaces it with `underwaterColor`. Values are clamped to 0–1 during initialization.]],
	readOnly = false,
	valuetype = "number",
}