return {
	type = "value",
	description = [[The animation group queued to play on the lower body when the current animation can transition. A value from the [`tes3.animationGroup`](https://mwse.github.io/MWSE/references/animation-groups/) namespace. A value of `255` indicates that no animation group is queued.]],
	valuetype = "tes3.animationGroup",
}
