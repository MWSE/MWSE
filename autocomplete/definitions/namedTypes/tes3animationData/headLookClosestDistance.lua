return {
	type = "value",
	description = [[The world-space distance, in game units, to the closest valid head-tracking target selected during the current animation update. This is used while evaluating potential targets so that nearer references take precedence, then reset after the procedural animation update.]],
	valuetype = "number",
}
