return {
	type = "value",
	description = [[The callback used every frame while the weather is active.]],
	readOnly = false,
	valuetype = "fun(weather: tes3weatherCustom, transitionScalar: number, deltaTime: number)",
}