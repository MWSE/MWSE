return {
	type = "value",
	description = [[If enabled, log colors will be enabled in the console. However, this will make the `mwse.log` file more difficult to read in a text editor.]],
	valuetype = "boolean",
	default = false,
}
