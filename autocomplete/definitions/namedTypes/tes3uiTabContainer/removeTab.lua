return {
	type = "method",
	description = [[Removes the tab with the given ID and destroys its associated contents block. If the removed tab was selected, the next available tab will be selected.]],
	arguments = {{
		name = "id",
		type = "string",
		description = "The unique identifier for the tab.",
	}},
}
