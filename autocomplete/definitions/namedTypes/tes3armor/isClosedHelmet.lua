return {
	type = "value",
	description = [[This property is `true` for closed helmets, which are unequippable for beast races. Closed helmets cover the head body part, while open helmets cover hair. Always `false` for armor other than helmets.]],
	readOnly = true,
	valuetype = "boolean",
}
