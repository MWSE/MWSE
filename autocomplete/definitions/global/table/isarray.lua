return {
	type = "function",
	description = [[Returns `true` when the given Lua table is a pure array-like Lua table, or `false` otherwise.]],
	arguments = {
		{ name = "t", type = "table" },
	},
	valuetype = "boolean",
}
